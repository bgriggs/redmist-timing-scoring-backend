name: Deploy Documentation

on:
  push:
    branches: [ main ]
    paths:
      - '**.cs'
      - '**.md'
      - 'docfx.json'
      - 'articles/**'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Checkout RedMist.TimingCommon
      uses: actions/checkout@v4
      with:
        repository: bgriggs/redmist-timing-common
        path: external/RedMist.TimingCommon

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
    
    - name: Setup Pages
      uses: actions/configure-pages@v4

    - name: Install DocFX
      run: dotnet tool install -g docfx

    - name: Build RedMist.TimingCommon
      run: dotnet build external/RedMist.TimingCommon/RedMist.TimingCommon/RedMist.TimingCommon.csproj --configuration Release /p:EmitCompilerGeneratedFiles=true

    - name: Build Solution
      run: dotnet build --configuration Release --no-incremental /p:EmitCompilerGeneratedFiles=true

    - name: Generate API Metadata
      run: docfx metadata docfx.json --property TargetFramework=net10.0 --warningsAsErrors false

    - name: Build Documentation
      run: docfx build docfx.json --warningsAsErrors false
    
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '_site'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
